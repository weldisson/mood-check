# mood-check

## Terraform
### Make the script executable
```bash
chmod +x load-env.sh
```

### Run Terraform commands through the script
```bash
./load-env.sh init
./load-env.sh plan
./load-env.sh apply
```